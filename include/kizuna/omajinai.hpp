#ifndef KIZUNA_OMAJINAI
#define KIZUNA_OMAJINAI
#include <iostream>

namespace kizuna
{
  inline auto omajinai ()
  {
    std::cin.tie (nullptr);
    std::ios_base::sync_with_stdio (false);
  }
} // namespace kizuna

#endif

